<form action="{{ form_action }}" method="post">
    <input type="hidden" name="token" value="{{ csrf_token('update_mission') }}">

    <button type="submit" name="action" value="finish" style="background-color: green; color: white;">Terminer</button>
    <button type="submit" name="action" value="accept" style="background-color: blue; color: white;">Accepter</button>
</form>